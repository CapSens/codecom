require 'spec_helper'

describe Capsens::Codecom do
  it 'has a version number' do
    expect(Capsens::Codecom::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
